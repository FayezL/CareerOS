{
  "name": "careeros",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3"
  },
  "lint-staged": {
    "frontend/**/*.{ts,tsx,js,jsx,json,css,md}": "prettier --write --ignore-unknown",
    "backend/**/*.py": [
      "uvx ruff check --fix",
      "uvx ruff format"
    ]
  }
}
