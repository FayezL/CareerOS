services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-careeros}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-careeros}
      POSTGRES_DB: ${POSTGRES_DB:-careeros}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      retries: 10
    ports:
      # Host port 5433 (not 5432) to avoid clashing with a native Windows
      # Postgres on 5432. Container port stays 5432 so the backend's internal
      # `db:5432` connection (DATABASE_URL) is unaffected. DBeaver uses 5433.
      - "5433:5432"

  backend:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - ./backend:/app
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      ENV: local
      LOG_LEVEL: INFO
      CORS_ORIGINS: http://localhost:3000
      CLERK_ISSUER: ${CLERK_ISSUER:-https://example.clerk.accounts.dev}
      CLERK_JWKS_URL: ${CLERK_JWKS_URL:-https://example.clerk.accounts.dev/.well-known/jwks.json}
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8000:8000"
    command: ["sh", "-c", "pip install -q uv && uv sync && uv run alembic upgrade head && uv run uvicorn careeros_api.main:app --host 0.0.0.0 --port 8000 --reload --reload-dir src"]

  frontend:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ./frontend:/app
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8000/api/v1
      # Server-side fetches (Server Components) run inside this container, where
      # `localhost:8000` is unreachable. Use the backend service hostname on the
      # Docker network instead. The public URL above stays for browser fetches.
      API_INTERNAL_URL: http://backend:8000/api/v1
      NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY:-pk_test_placeholder}
      CLERK_SECRET_KEY: ${CLERK_SECRET_KEY:-}
    depends_on:
      - backend
    ports:
      - "3000:3000"
    command: ["sh", "-c", "corepack enable && pnpm install && pnpm dev"]

volumes:
  pgdata:

networks:
  default:
